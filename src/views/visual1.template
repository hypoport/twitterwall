
<div class="container">
  <div>
    <ul>
      <li ng-repeat="tweet in tweets">
        <div class="well tweet-box">
          <div class="tweet-text-and-user">
            <div class="tweet-text pull-left" id="tweetText{{$index}}">{{tweet.text}}</div>
            <span class="pull-right">
              <img ng-src="{{tweet.user.originalProfileImageURL}}" alt="avatar" width="64px">
              <br style="clear:both">
              <span class=" tweet-user">{{tweet.user.screenName}}</span>
            </span>

            <div class="clearfix"></div>
          </div>
        </div>
      </li>
    </ul>
  </div>

</div>
